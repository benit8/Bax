{
	let i = 0;
	const a = ['a', 'b', 'c', 'd'];
	static const counter = 0;

	/*const fizzbuzz = function (Number maximum) {
		for (i : range(0, maximum)) {
			println(match (0) {
				i % 15  => "FizzBuzz",
				i % 3   => "Fizz",
				i % 5   => "Buzz",
				default => i.toString(),
			});
		}
	};*/
}
