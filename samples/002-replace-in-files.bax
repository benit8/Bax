let main = fn () {
	FsFinder.new()
		.in(["include", "sources"])
		.files()
		.name(["*.cpp", "*.hpp"])
		.find()
		->each(fn (filename) {
			let file = FsFile.open(filename);
			let contents = file.read_all()
				->replace("2021", "2021-2022");
			file.rewind();
			file.write(contents);
		});
};
